repository:
  id: repo.knowledge-experiences
  github: matuteiglesias/knowledge-experiences
  lifecycle: active
  role: knowledge experience composition authority

authority:
  owns:
    - collection membership recipes and frozen collection releases
    - experience composition recipes and frozen experience releases
    - renderer capability declarations and adapter handoff semantics
    - reproducibility/provenance of experience builds
  does_not_own:
    - producer domain semantics or producer-owned schemas
    - shared knowledge interoperability contracts
    - evidence truth or evidence-selection policy
    - semantic inspection or inference
    - renderer implementation internals
    - editorial claims, intellectual stance, or automated publication judgment

ecosystem:
  architecture_authority: repo.knowledge-ecosystem-docs
  shared_interop_authority: repo.kb-contracts
  proven_producers:
    - repo.paper-kb
  expected_capabilities:
    - repo.knowledge-inspect
    - repo.kb-artifacts
  expected_renderers:
    - repo.abstract-scroller
  historical_or_vertical_experiences:
    - repo.thesis
    - repo.journal

interfaces:
  implemented_v1:
    - knowledge.collection-spec@1
    - knowledge.collection-release@1
    - knowledge.experience-spec@1
    - knowledge.experience-release@1
  source_adapters:
    - jsonl
    - paper-catalog-jsonl
  producer_inputs:
    - paper.catalog-record@1
  selection_modes:
    - all
    - ids
    - facets
  renderers:
    - static-navigator

verification:
  proven:
    - deterministic local fixture builds through kx doctor
    - pinned Paper KB paper.catalog-record@1 output composes into static navigator experiences in cross-repository CI
    - author facet selection produces a second experience from the same producer release without new renderer code
  limitations:
    - cross-repository proof uses sanitized producer-generated artifacts, not a rights-sensitive real paper corpus
    - working-paper-series experience is not claimed because current Paper KB venue/series coverage is not guaranteed

status:
  phase: v1.2-paper-kb-catalog-seam
  next_gate: v1.3-abstract-scroller-handoff
  implementation_policy: evidence-pulled
