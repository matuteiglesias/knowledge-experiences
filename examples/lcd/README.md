# LCD institutional index proof

V1.4 Lane A reuses the existing run-scoped `title_slug_index.json` from `lcd-UBA-knowledgebase`; no new LCD domain contract is invented here.

CI copies the exact artifact from pinned LCD commit `dcaa61e338b284d43764b9c06a0f112629e958f6`, run `local_contract_check_20260424T161417Z`, and builds a static navigator.

The checked-in LCD run is trusted by its own registry but is named and documented as a local contract check. Therefore this example proves the integration mechanics only; it does not claim a live-source institutional corpus E1/E2.
